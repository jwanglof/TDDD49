<UserControl x:Class="Poker.Player"
             x:Name="userControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Poker"
             Height="70">

    <Grid x:Name="playerGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <local:Money x:Name="money" Grid.Column="0" VerticalAlignment="Center"></local:Money>
        <Grid x:Name="cardGrid" Grid.Column="1" IsEnabled="False">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="70"/>
            </Grid.ColumnDefinitions>
        </Grid>
        <Grid x:Name="turnGrid" Grid.Column="2" Visibility="Hidden">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Label Grid.Row="0">Your turn!</Label>
            <Button x:Name="btnDone" Content="Done"  Grid.Row="1" />
        </Grid>
    </Grid>
</UserControl>
